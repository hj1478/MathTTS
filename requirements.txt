# Direct dependencies only, hand-written (the old file was a full `pip freeze`
# of the dev venv and only installed on macOS). Everything else in the venv is a
# transitive dependency of these. Developed on macOS arm64 / Python 3.11; all
# packages below publish Linux and Windows wheels too.
#
# Node side is separate: `npm install` inside sre-probe/ (stage 3).

# --- Stage 1: OCR (ocr_vl.py) ---------------------------------------------
# paddlex[ocr] is required explicitly: paddleocr alone raises DependencyError
# at pipeline creation (verified with paddleocr 3.7.0 / paddlex 3.7.2).
paddleocr==3.7.0
paddlex[ocr]==3.7.2
paddlepaddle==3.3.1

# --- Stage 0 (optional): PDF -> page images (pdf_to_images.py) ------------
pypdfium2==5.11.0

# --- Stage 4: Azure TTS (tts_full.py, tts_probe.py) -----------------------
# The only package stage 4 needs — tts_full.py is runnable without the
# paddle/OCR stack above.
azure-cognitiveservices-speech==1.50.0

# --- Eval harnesses (llm.py, inbox_eval.py, dot_check.py) -----------------
requests==2.34.2

# --- Tests (tests/) -------------------------------------------------------
pytest==9.1.1
