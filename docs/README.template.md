<!--
=============================================================================
README 작성 템플릿                                        (관련 이슈: #12, #10)
=============================================================================

사용법
  1. 이 파일을 저장소 루트의 README.md 로 복사한다. (이 파일 자체는 그대로 둔다)
  2. 위에서부터 각 절의 "작성 안내" 주석을 읽고 그 아래 TODO 를 채운다.
  3. 채운 절의 안내 주석은 지운다. 남아 있는 안내 주석 = 아직 안 쓴 절.
  4. 다 채운 뒤 이 파일 맨 아래 "완성 판정" 목록으로 스스로 점검한다.

이 템플릿은 구조와 질문만 준다. 내용은 직접 쓴다.
연구 질문, 실패한 시도, 결과는 이 프로젝트를 만든 사람만 쓸 수 있는 부분이다.

순서에 대해
  아래 순서는 "안 돌려볼 독자"를 기준으로 정했다. 문제 -> 질문 -> 방법 ->
  결과 순이다. 읽는 사람은 이 순서로 읽지만, 쓰기는 6번(예시 하나)이나
  9번(검증한 환경)처럼 쉬운 절부터 시작해도 된다.

언어
  안내와 제목은 한국어다. 영어로 쓰기로 정했다면 제목을 영어로 바꾸고,
  그 결정을 한 줄로 밝혀 둔다. 섞이는 것만 피하면 된다.

분량
  전체 두 화면을 목표로 한다. 넘치면 단계별 세부는 docs/ 아래 별도 파일로
  옮기고 README 에서는 링크만 남긴다.
=============================================================================
-->

# MathTTS

<!-- 작성 안내 — 한 줄 요약
     제목 바로 아래 한 문장. 무엇을 하는 것인지만.
     지금 저장소 설명에 이미 한 줄이 있다. 그걸 쓰거나 더 나은 문장으로 고친다.
     여기에 동기나 배경을 넣지 않는다. 그건 다음 절이다.
-->

TODO

## 이 프로젝트가 푸는 문제

<!-- 작성 안내 — 3~5문장
     답해야 하는 질문:
       - 누가, 어떤 상황에서 곤란한가?
       - 지금은 그 사람이 어떻게 하고 있고, 왜 그것으로 부족한가?
       - 이게 해결되면 무엇이 달라지는가?

     이 절이 저장소 전체에서 지금 완전히 비어 있는 부분이다.
     읽는 사람은 이걸 모르면 나머지가 왜 필요한지 알 수 없다.

     흔한 실수: 기술 설명으로 시작하는 것("OCR과 TTS를 연결하여...").
     기술이 아니라 사람과 상황으로 시작한다.
-->

TODO

## 연구 질문

<!-- 작성 안내 — 질문 2~4개, 각각 한두 문장
     이 절이 이 문서를 "만든 것 소개"가 아니라 "연구"로 만든다.

     답해야 하는 질문:
       - 시작할 때 확인하려던 게 정확히 무엇이었나?
       - 어떤 결과가 나오면 성공이고 어떤 결과면 실패인가?
       - 시작할 때는 몰랐던 것 / 될지 안 될지 확신할 수 없던 것은 무엇이었나?

     찾는 방법: 이미 답을 구하려고 만든 도구가 저장소에 두 개 있다.
       - sre-probe/index.js 는 어떤 질문에 답하려고 만들었나?
       - tts_probe.py 의 네 개 테스트 문장은 각각 무엇을 확인하려는 것인가?
     그 질문들을 여기 옮겨 적으면 된다. 이미 머릿속에 있는 것을 꺼내는 작업이다.

     주의: 답을 여기 쓰지 않는다. 답은 "결과" 절에 쓴다.
-->

TODO

## 접근 방법

<!-- 작성 안내 — 표 + 한 문단
     먼저 단계 표를 채운다. 아래 첫 행은 형식을 보여주기 위한 예시다.
     나머지 행은 직접 채우고, 첫 행도 자기 표현으로 고쳐 쓴다.

     그 다음 표 아래에 한 문단. 답해야 하는 질문:
       - 왜 이렇게 여러 단계로 나누었는가?
       - OCR 결과를 바로 TTS 에 넣으면 왜 안 되는가?
       - 2단계(정규화)와 3단계(수식 발화)가 각각 없으면 무엇이 깨지는가?

     이 문단이 중요한 이유: 단계 목록은 코드를 보면 알 수 있지만,
     왜 그렇게 나누었는지는 코드가 말해주지 못한다.
-->

| 단계 | 실행 파일 | 입력 → 출력 | 하는 일 |
|---|---|---|---|
| 1. OCR | `ocr_vl.py` | `*.png` → `output/*.md` | TODO (예시 행 — 직접 고쳐 쓴다) |
| 2. TODO | TODO | TODO | TODO |
| 3. TODO | TODO | TODO | TODO |
| 4. TODO | TODO | TODO | TODO |

TODO — 왜 이 구조인지 한 문단

<!-- 작성 안내 — 조사용 도구는 단계와 구분해서 적는다
     sre-probe/index.js 와 tts_probe.py 는 파이프라인 단계가 아니라
     확인용 도구다. 표에 섞지 말고 아래에 따로 한두 줄로 적는다.
     지금은 이 구분이 각 파일 docstring 안에만 있어서, 저장소를 처음 보는
     사람은 파일 6개가 모두 파이프라인인 줄 안다.
-->

TODO — 조사용 도구 2개

## 결과

<!-- 작성 안내 — 이 절이 가장 자주 비어 있고, 가장 중요하다
     답해야 하는 질문:
       - 위에 적은 각 연구 질문에 대해, 답이 무엇이었나?
       - 어디까지 됐고 어디서 막혔나?
       - 그 판단의 근거는 무엇인가? (무엇을 보고/듣고 그렇게 판단했나)
       - 예상과 달랐던 것은 무엇인가?

     원칙 하나만 지킨다: 실제로 확인한 것과 추측한 것을 구분해서 쓴다.
     코드 주석에서 이미 하고 있는 방식이다
     ("confirmed from real output, not assumed" / "verify by ear before trusting").
     확인하지 않은 것은 "아직 확인하지 않음"이라고 적으면 된다. 그래도 된다.
     오히려 그렇게 적힌 문서가 더 믿을 만하다.

     안 된 것도 같은 무게로 쓴다. 연구에서 실패는 부록이 아니라 결과다.
-->

TODO

## 예시 하나

<!-- 작성 안내 — 문제 한 개를 골라 전 단계를 그대로 보여준다
     원본 이미지 -> OCR 출력 -> 정규화된 LaTeX -> 한국어 발화문 -> 음성 파일.
     각 단계의 실제 출력을 코드 블록에 붙인다. 설명은 최소한으로.

     왜 하는가: 설명 한 단락보다 이해가 빠르고, 나중에 뭔가 바뀌었을 때
     "전에는 이렇게 나왔다"는 기준으로도 쓸 수 있다.

     고를 문제: kr question 1.png 이 후보다. 이 문제의 OCR 출력에는
     괄호가 깨지는 실제 사례가 들어 있어서, 정규화 단계가 무엇을 고치는지가
     예시 안에서 그대로 드러난다. 잘 되는 예시보다 이런 예시가 더 유용하다.
-->

TODO

## 실행 방법

<!-- 작성 안내 — 처음 받은 사람이 그대로 따라 할 수 있어야 한다 (이슈 #10)
     빠뜨리기 쉬운 것들:
       - Python 의존성 설치. 지금 requirements.txt 는 macOS 전용 패키지 때문에
         다른 OS 에서 설치가 안 된다. 그 사실을 적거나, 이슈 #7 을 먼저 처리한다.
       - sre-probe/ 안에서 따로 npm install 이 필요하다는 것.
         별개의 의존성이라 가장 자주 누락된다.
       - 자격증명. 어떤 환경변수가 필요한지, .env 를 어디에 두는지.
         .env.example 이 이미 있으니 그걸 가리킨다. 키 값은 절대 적지 않는다.
       - 4단계를 순서대로 실행하는 명령. 폴더 이름이 단계 사이에 맞아야 한다는 것,
         그리고 3단계는 sre-probe/ 안에서 실행해야 한다는 것.
       - 첫 실행 비용: 1단계는 처음에 모델 가중치를 수백 MB 내려받고,
         CPU 에서는 이미지 한 장에 수십 초가 걸린다. 미리 알려주지 않으면
         멈춘 것으로 오해한다.
       - 어느 단계가 인터넷/유료 서비스를 쓰고 어느 단계가 완전히 로컬인지.

     명령은 직접 한 번 실행해 보고 적는다. 기억으로 적으면 틀린다.
-->

TODO

## 한계와 아직 확인하지 않은 것

<!-- 작성 안내 — 이 절이 있으면 문서 전체가 더 믿을 만해진다
     세 가지를 구분해서 적는다:
       - 의도적으로 처리하지 않은 것 (그리고 왜 그렇게 정했는지)
       - 아직 확인하지 않은 것
       - 알려진 실패 / 잘 안 되는 입력

     쓰기 쉬운 절이다. 이미 코드 안에 있는 내용을 옮겨오면 된다.
     normalize.py docstring 의 "DOESN'T" 목록,
     sre-probe/speak.js 의 say-as 관련 미확인 사항 같은 것들.

     하지 말아야 할 것: 코드가 못 하는 것을 문서에서 된다고 쓰는 것.
     예를 들어 저장소에 PDF 파일이 들어 있지만 PDF 를 처리하는 코드는 없다.
     문서에서 PDF 입력이 된다고 쓰면 읽는 사람은 그 말을 믿는다. (이슈 #8)
-->

TODO

## 검증한 환경

<!-- 작성 안내 — 버전 + 날짜 + 플랫폼
     왜 필요한가: "SRE 는 한국어를 지원한다"는 유통기한이 있는 문장이다.
     "SRE 5.0.0-rc.4 에서 2026-07 에 확인" 은 기록이 된다.
     반 년 뒤에 뭔가 안 될 때, 무엇이 바뀌었는지 비교할 기준이 이것뿐이다.

     이미 코드 주석에서 하고 있는 방식이니 그대로 표로 옮기면 된다.
     확인한 날짜를 반드시 같이 적는다.
-->

| 항목 | 버전 | 확인한 날짜 |
|---|---|---|
| TODO | TODO | TODO |

실행/확인한 환경: TODO (OS, CPU 또는 GPU)

## 사용한 도구

<!-- 작성 안내 — 이름과 역할만 한 줄씩. 링크 하나씩.
     각 도구가 이 파이프라인에서 무엇을 담당하는지만 적는다.
     이 절은 짧아도 된다.
-->

TODO

<!--
=============================================================================
완성 판정 — 다 쓴 뒤 스스로 점검
=============================================================================
소스 코드를 열지 않은 사람이 이 README 만 읽고 아래에 답할 수 있으면 된 것이다.

  [ ] 이게 누구의 어떤 문제를 푸는지 한 문장으로 말할 수 있다
  [ ] 무엇을 확인하려는 연구였는지 말할 수 있다
  [ ] 됐는지 안 됐는지, 그리고 그 근거를 말할 수 있다
  [ ] 시도했지만 안 된 것이 무엇인지 말할 수 있다
  [ ] 자기 컴퓨터에서 문제 하나를 음성까지 돌려볼 수 있고, 첫 실행에
      시간과 용량이 얼마나 드는지 미리 안다
  [ ] 어느 부분이 안 되는지 / 확인되지 않았는지 구분할 수 있다
  [ ] 어떤 주장이 실제로 검증된 것이고, 어떤 버전에서 언제 확인한 것인지 안다

그리고 작성자 본인에게 해당하는 항목 하나:

  [ ] 반 년 뒤에 이 README 를 다시 읽고, 왜 수식을 $...$ 로 통일했는지와
      왜 engine="transformers" 를 넣으면 안 되는지 여전히 알 수 있다
      (그 두 가지는 지금 저장소에서 유실된 상태다 — 이슈 #3)

한 항목이라도 답이 안 나오면 그 절이 아직 덜 쓰인 것이다.
=============================================================================
-->
